<Window x:Class="Puissance4.Interface.PopUpSauvegarde"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Puissance4.Interface"
        mc:Ignorable="d"
        Title="Puissance 4" Height="400" Width="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Background="#FF0A111F">
            <Button x:Name="BtnSave1" Content="Sauvegarde 1" Height="40" Background="#CBC7B7" Margin="20, 10" 
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave2" Content="Sauvegarde 2" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave3" Content="Sauvegarde 3" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave4" Content="Sauvegarde 4" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave5" Content="Sauvegarde 5" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave6" Content="Sauvegarde 6" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave7" Content="Sauvegarde 7" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave8" Content="Sauvegarde 8" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave9" Content="Sauvegarde 9" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="BtnSave10" Content="Sauvegarde 10" Height="40" Background="#CBC7B7" Margin="20, 10"
                    Foreground="#FF0A111F" FontFamily="{StaticResource PlaypenSans}" FontSize="20">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </ScrollViewer>
</Window>
